import {GoogleSpreadsheet} from 'google-spreadsheet';

/*Takes information from body and sends POST request
  Returns: (bool) True if successful entry into spreadsheet, false if not
*/
const sendEmail = async (body)  => {
  //TODO
}

export default sendEmail;