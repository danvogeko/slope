import {GoogleSpreadsheet} from 'google-spreadsheet';

/*Takes information from body and sends POST request to create user in spreadsheet
  Returns: (bool) True if successful entry into spreadsheet, false if not
*/
const createUser = async (body)  => {
  //TODO
}

export default createUser;